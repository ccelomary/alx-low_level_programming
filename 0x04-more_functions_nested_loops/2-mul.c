/**
 * mul - function that takes two numbers and return product of them
 *
 * @a: first parameter integer
 * @b: second parameter integer
 * Return: multiplication of a and b
 */
int	mul(int a, int b)
{
	return (a * b);
}
