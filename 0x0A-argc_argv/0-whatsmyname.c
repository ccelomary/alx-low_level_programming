#include <stdio.h>
/**
 * main - prints the name of the program
 *
 * @argc: length of the arguments
 * @argv: array contains agruments
 * Return: Success 0
 */
int	main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
